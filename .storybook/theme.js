import { styled, create } from '@storybook/theming';

import logo from '../assets/logo.svg';

export default create({
  base: 'light',
  brandTitle: 'Material Components storybook',
  brandImage: logo,
  appContentBg: '#f7fafc'
});
